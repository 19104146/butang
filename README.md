# butang

```sh
git clone https://github.com/19104146/butang.git
cd butang
npm install # install dependencies
git checkout dev # change branch to dev
npx husky # execute only once; runs prettier and eslint pre-commit
npx expo start -c # start server
```
